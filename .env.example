# RTMP Gateway Configuration

# Environment: "development" or "production"
# - development: Human-readable logs, debug level by default (unless DEBUG=false)
# - production: JSON logs, info level (unless DEBUG=true)
ENV=development

# Debug Mode: "true" or "false"
# Force enables debug logging regardless of ENV
DEBUG=true

# Redis Configuration (Required for Traffic Logging & Cache Invalidation)
REDIS_ADDR=localhost:6379

# L3 Stream Info API URL
# If not set, defaults to internal mock API: http://localhost:8081/mock_l3
# The API should accept ?key=<path> and return structured JSON.
# Example: http://my-backend-service/api/stream
STREAM_API_URL=

# RTMP Listening Port (Default: :1935)
RTMP_ADDR=:1935

# Health Check & Mock API Port (Default: :8081)
HTTP_ADDR=:8081
